
<% @items.each do |item| %>
<div class="item_container"> 
	<div class="image_box center">
 		<%= link_to(image_tag(item.photo.url(:small), :title => "#{item.title}"), @item.photo.url(:large), :class => 'cloud-zoom', :id => 'zoom1', :rel => "position: 'inside'") %>
 	</div>
 	<dt><%= link_to item.title, :action => "show", :id => item -%></dt>
 	<p><b>dimensions: </b><%= item.dimension -%></p>
 	<p><b>price: </b>

 		<%= sprintf("%0.2f", item.price) -%>

 		<p>
			<strong>
				<%= add_item_link("add", item) -%>
			</strong>		
		</p>	
 </div>

<% end %>

